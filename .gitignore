node_modules/
dist/
*.tsbuildinfo
*.tgz
.env
bin/*
!bin/electron-mcp.mjs
build-tmp/
dist-release/

# npm auth token -- never commit (a project-local .npmrc can be
# created by tooling and would carry a live automation token)
.npmrc
